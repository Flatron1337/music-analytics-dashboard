# 📋 Music Analytics Dashboard — Roadmap & TODO List

> Документ обновляется в реальном времени. Здесь зафиксировано, на чём мы остановились, что уже готово и какие задачи стоят в очереди.

---

## 🎯 Текущий фокус (Current Focus)
> **Фаза 5: 🧠 AI-ассистент классификации жанров (Dual-Engine: Gemini + Groq)**  
> Модуль `ai_genre_classifier.py`, консольный скрипт `run_ai_enrichment.py`, SSE-эндпоинт в `api.py` и карточка `AiEnrichmentCard` во Flutter-клиенте реализованы и протестированы!  
> **Следующий шаг:** Запуск полного AI-обогащения базы и переход к бэклогу (Android APK, аудио-превью).

---

## ⏳ В работе (In Progress)

- [x] **AI Genre Classifier Module (`ai_genre_classifier.py`)**:
  - [x] Реализовать двухъядерный клиент: **Primary = Gemini 3.6 Flash**, **Fallback = Groq Qwen 3.8 / LLaMA**.
  - [x] Механизм извлечения реальных треков исполнителя из медиатеки (на примере `Chickencore` передаются реальные треки *«Роботы создают хардтемпо»*, *«Dance 4 Floor»*, безошибочно определяющие *Dubstep & EDM*).
  - [x] Пакетная отправка (батчинг по 35 артистов в одном JSON-запросе).
  - [x] Сохранение результата в `genre_cache.sqlite` с меткой `source = 'ai'` и детальными тегами.
- [x] **Консольный скрипт запуска (`run_ai_enrichment.py`)**:
  - [x] Разовый прогрев и обогащение всех артистов в базе с визуальным прогресс-баром и статистикой до/после.
- [x] **Интеграция в Backend API (`api.py`)**:
  - [x] Эндпоинты `GET /api/enrich-genres/status` и `GET/POST /api/enrich-genres/stream` (SSE-стриминг прогресса AI-классификации).
  - [x] Автоматический пересчёт сводных жанровых стат в `get_dataset()` и `_classifier.reload_memory_cache()` после обогащения.
- [x] **Интерфейс мобилки и веб-клиента (`yndex_mobile`)**:
  - [x] Карточка `AiEnrichmentCard` в разделе «Профиль» с бейджем количества неразмеченных треков.
  - [x] Кнопка запуска и живой прогресс-бар классификации с автоматическим обновлением графиков.

---

## ✅ Выполнено (Completed)

### 🧠 Фаза 5: AI-ассистент классификации андерграунд-треков
- [x] Двухъядерный модуль `ai_genre_classifier.py` с автопереключением Gemini ➔ Groq.
- [x] Скрипт `run_ai_enrichment.py` для фонового и пакетного анализа.
- [x] Потоковые SSE-эндпоинты в `api.py`.
- [x] Интерактивный UI-компонент `AiEnrichmentCard` в мобильном приложении.

### 🚀 Фаза 1: Экспорт умных плейлистов в Яндекс Музыку
- [x] Готовые смарт-пресеты:
  - ⚡ *Жанровые кластеры* (Чистый Phonk, Heavy Metal, Dubstep и др.).
  - 🤝 *Фитотека* (Все совместные треки и коллаборации).
  - 💎 *Скрытые жемчужины* (Треки авторов, у которых всего 1–2 лайка).
  - ⏳ *Золотая эра* (Первые 100 треков вашей коллекции).
- [x] Исправление ошибки `wrong-json`: санитарная очистка и строгая валидация `album_id`, пропуск треков без альбома.
- [x] Батчинг при создании плейлистов (пакетная вставка по 150–200 треков для обхода лимитов Яндекс Музыки).
- [x] Быстрые кнопки экспорта в мобильном приложении и на веб-сайте.

### 🌐 Фаза 2: Архитектура, веб-версия и облачный деплой
- [x] Единый асинхронный шлюз (`main.py` на `aiohttp`):
  - Порт `10000`: внешний вход на Render.
  - Порт `8501`: Streamlit Desktop Web UI (маршрут `/`).
  - Порт `5001`: Flask REST API (маршрут `/api/*`).
  - Проксирование WebSockets для интерактивных компонентов Streamlit.
- [x] Персистентная авторизация:
  - Токен больше не сбрасывается при закрытии вкладки или перезагрузке (сохранение в `localStorage`, `SharedPreferences` и файле `.yandex_token`).
- [x] Чистый старт:
  - Убрана автозагрузка устаревшего жестко зашитого файла плейлиста; страница чистая и ожидает подключения аккаунта.
- [x] Автоматический деплой на Render (`render.yaml`) в статусе `live`.

### 📸 Фаза 3: Персональная Story (9:16) для соцсетей
- [x] Генератор карточки Story в стиле Spotify Wrapped / Yandex Recap:
  - Определение музыкального архетипа (*«Электронный Рейвер»*, *«Повелитель Тяжести»*, *«Фонк Дрифтер»*).
  - Метрики: общий хронометраж, число треков, процент фитов, топ-3 артиста, долевые бейджи жанров.
- [x] Кроссплатформенный модуль сохранения изображений (`story_saver.dart`):
  - В браузере / PWA: генерация Blob и прямое скачивание PNG в системную папку **«Загрузки»** устройства.
  - В Android: сохранение в `/storage/emulated/0/Download`.
  - Информативное уведомление с точным путём сохранения.

### ⚡ Фаза 4: Потоковая синхронизация и граф коллабораций
- [x] Исправление таймаута синхронизации (`TimeoutException after 0:00:15.000000`):
  - Шлюз `main.py` переведен на честный unbuffered стриминг через `web.StreamResponse` + `iter_any()`.
  - Таймаут на мобильном клиенте увеличен до 60 секунд.
- [x] Потоковая трансляция прогресса через SSE (Server-Sent Events) с выводом этапов (*Авторизация ➔ Сбор треков ➔ Классификация ➔ Готово*).
- [x] Интерактивный граф связей (карта фитов):
  - Бэкенд эндпоинт `/api/collaborations-graph`.
  - Визуализация связей между артистами на базе `CustomPainter` с физикой узлов, зумом и модальным окном деталей артиста.

### 📚 Документация и стиль
- [x] Создан стильный главный [README.md](file:///e:/python/README.md) с бейджами, диаграммой архитектуры и инструкциями запуска.
- [x] Создан мобильный [yndex_mobile/README.md](file:///e:/python/yndex_mobile/README.md) с полным описанием экранов и возможностей.

---

### 🚀 Фаза 5: Реализация всех бэклог-функций (Backlog Completion)
- [x] **Сборка нативного Android APK**:
  - Настроено окружение с JDK 21 (`E:\Android_studio\jbr`) и Android SDK 35 (`E:\DevCache\Android\Sdk`).
  - Создан скрипт автоматической сборки [yndex_mobile/build_apk.bat](file:///e:/python/yndex_mobile/build_apk.bat).
  - Успешно собран нативный оптимизированный `app-release.apk` (54 МБ) для прямой установки на Android-смартфоны.
- [x] **Превью треков (Audio Preview Player)**:
  - Бэкенд эндпоинт `GET /api/track-stream/<track_id>` с получением прямого потока MP3 через `tracks_download_info(..., get_direct_links=True)`.
  - Мобильный сервис `AudioPlayerService` на базе пакета `audioplayers` (6.8.1).
  - Floating `MiniPlayerBar` с анимацией прогресса, паузой/возобновлением, индикацией загрузки и кнопками быстрого прослушивания прямо из каталога.
- [x] **Аудио-характеристики треков (Audio Features Profile)**:
  - Бэкенд эндпоинт `GET /api/audio-features`: расчет индекса драйва/энергии (81.2% "Максимальный драйв 🔥"), распределение по 4 диапазонам BPM (150-180+, 130-150, 100-130, <100) и уровни громкости LUFS (EBU R128).
  - Интерактивный виджет `AudioFeaturesCard` на главной вкладке "Музыкальный обзор" с неоновыми шкалами и шкалой фитов.
- [x] **Экспорт графа связей**:
  - Бэкенд эндпоинт `GET /api/collaborations-graph/export-html`: генерация автономного self-contained HTML (Vis.js Network) с неоновым темным оформлением, физикой и легендой жанров.
  - Кнопка «Экспорт HTML» в приложении на экране графа коллабораций с автоматическим сохранением и открытием в браузере.
- [x] **Поиск дубликатов треков**:
  - Бэкенд эндпоинт `GET /api/duplicates`: интеллектуальная нормализация названий (очистка `(Remastered)`, `(Deluxe)`, `(VIP)` и т.д.), выявившая 140 дубликатов в 107 группах с подсчетом суммарного сэкономленного времени.
  - Экран `DuplicatesScreen` с поиском, группировкой по трекам, бейджами "Оригинал / Дубликат", кнопками прямого прослушивания превью и перехода в Яндекс Музыку.
  - Карточка перехода к дубликатам во вкладке профиля.

---

## 🔮 Бэклог и будущие идеи (Backlog)
- [ ] Офлайн-кэширование метаданных в SQLite / Hive для мгновенного запуска без сети.
- [ ] Автоматическое создание плейлиста "Удалить дубликаты" через Яндекс API.
- [ ] Виджет для рабочего стола Android (Glance / Android AppWidget) с текущей статистикой.
